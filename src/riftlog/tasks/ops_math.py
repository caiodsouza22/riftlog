"""Generated affine/step math callables with distinct coefficients."""
from __future__ import annotations

from riftlog.application.catalog import task

@task('math.affine_001')
def affine_001(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 2 + -2) * scale

@task('math.affine_002')
def affine_002(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 3 + -1) * scale

@task('math.affine_003')
def affine_003(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 4 + 0) * scale

@task('math.affine_004')
def affine_004(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 5 + 1) * scale

@task('math.affine_005')
def affine_005(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 6 + 2) * scale

@task('math.affine_006')
def affine_006(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 7 + 3) * scale

@task('math.affine_007')
def affine_007(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 8 + -3) * scale

@task('math.affine_008')
def affine_008(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 9 + -2) * scale

@task('math.affine_009')
def affine_009(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 10 + -1) * scale

@task('math.affine_010')
def affine_010(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 11 + 0) * scale

@task('math.affine_011')
def affine_011(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 1 + 1) * scale

@task('math.affine_012')
def affine_012(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 2 + 2) * scale

@task('math.affine_013')
def affine_013(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 3 + 3) * scale

@task('math.affine_014')
def affine_014(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 4 + -3) * scale

@task('math.affine_015')
def affine_015(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 5 + -2) * scale

@task('math.affine_016')
def affine_016(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 6 + -1) * scale

@task('math.affine_017')
def affine_017(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 7 + 0) * scale

@task('math.affine_018')
def affine_018(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 8 + 1) * scale

@task('math.affine_019')
def affine_019(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 9 + 2) * scale

@task('math.affine_020')
def affine_020(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 10 + 3) * scale

@task('math.affine_021')
def affine_021(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 11 + -3) * scale

@task('math.affine_022')
def affine_022(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 1 + -2) * scale

@task('math.affine_023')
def affine_023(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 2 + -1) * scale

@task('math.affine_024')
def affine_024(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 3 + 0) * scale

@task('math.affine_025')
def affine_025(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 4 + 1) * scale

@task('math.affine_026')
def affine_026(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 5 + 2) * scale

@task('math.affine_027')
def affine_027(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 6 + 3) * scale

@task('math.affine_028')
def affine_028(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 7 + -3) * scale

@task('math.affine_029')
def affine_029(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 8 + -2) * scale

@task('math.affine_030')
def affine_030(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 9 + -1) * scale

@task('math.affine_031')
def affine_031(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 10 + 0) * scale

@task('math.affine_032')
def affine_032(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 11 + 1) * scale

@task('math.affine_033')
def affine_033(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 1 + 2) * scale

@task('math.affine_034')
def affine_034(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 2 + 3) * scale

@task('math.affine_035')
def affine_035(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 3 + -3) * scale

@task('math.affine_036')
def affine_036(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 4 + -2) * scale

@task('math.affine_037')
def affine_037(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 5 + -1) * scale

@task('math.affine_038')
def affine_038(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 6 + 0) * scale

@task('math.affine_039')
def affine_039(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 7 + 1) * scale

@task('math.affine_040')
def affine_040(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 8 + 2) * scale

@task('math.affine_041')
def affine_041(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 9 + 3) * scale

@task('math.affine_042')
def affine_042(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 10 + -3) * scale

@task('math.affine_043')
def affine_043(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 11 + -2) * scale

@task('math.affine_044')
def affine_044(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 1 + -1) * scale

@task('math.affine_045')
def affine_045(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 2 + 0) * scale

@task('math.affine_046')
def affine_046(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 3 + 1) * scale

@task('math.affine_047')
def affine_047(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 4 + 2) * scale

@task('math.affine_048')
def affine_048(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 5 + 3) * scale

@task('math.affine_049')
def affine_049(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 6 + -3) * scale

@task('math.affine_050')
def affine_050(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 7 + -2) * scale

@task('math.affine_051')
def affine_051(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 8 + -1) * scale

@task('math.affine_052')
def affine_052(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 9 + 0) * scale

@task('math.affine_053')
def affine_053(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 10 + 1) * scale

@task('math.affine_054')
def affine_054(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 11 + 2) * scale

@task('math.affine_055')
def affine_055(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 1 + 3) * scale

@task('math.affine_056')
def affine_056(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 2 + -3) * scale

@task('math.affine_057')
def affine_057(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 3 + -2) * scale

@task('math.affine_058')
def affine_058(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 4 + -1) * scale

@task('math.affine_059')
def affine_059(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 5 + 0) * scale

@task('math.affine_060')
def affine_060(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 6 + 1) * scale

@task('math.affine_061')
def affine_061(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 7 + 2) * scale

@task('math.affine_062')
def affine_062(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 8 + 3) * scale

@task('math.affine_063')
def affine_063(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 9 + -3) * scale

@task('math.affine_064')
def affine_064(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 10 + -2) * scale

@task('math.affine_065')
def affine_065(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 11 + -1) * scale

@task('math.affine_066')
def affine_066(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 1 + 0) * scale

@task('math.affine_067')
def affine_067(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 2 + 1) * scale

@task('math.affine_068')
def affine_068(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 3 + 2) * scale

@task('math.affine_069')
def affine_069(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 4 + 3) * scale

@task('math.affine_070')
def affine_070(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 5 + -3) * scale

@task('math.affine_071')
def affine_071(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 6 + -2) * scale

@task('math.affine_072')
def affine_072(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 7 + -1) * scale

@task('math.affine_073')
def affine_073(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 8 + 0) * scale

@task('math.affine_074')
def affine_074(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 9 + 1) * scale

@task('math.affine_075')
def affine_075(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 10 + 2) * scale

@task('math.affine_076')
def affine_076(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 11 + 3) * scale

@task('math.affine_077')
def affine_077(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 1 + -3) * scale

@task('math.affine_078')
def affine_078(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 2 + -2) * scale

@task('math.affine_079')
def affine_079(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 3 + -1) * scale

@task('math.affine_080')
def affine_080(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 4 + 0) * scale

@task('math.affine_081')
def affine_081(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 5 + 1) * scale

@task('math.affine_082')
def affine_082(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 6 + 2) * scale

@task('math.affine_083')
def affine_083(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 7 + 3) * scale

@task('math.affine_084')
def affine_084(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 8 + -3) * scale

@task('math.affine_085')
def affine_085(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 9 + -2) * scale

@task('math.affine_086')
def affine_086(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 10 + -1) * scale

@task('math.affine_087')
def affine_087(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 11 + 0) * scale

@task('math.affine_088')
def affine_088(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 1 + 1) * scale

@task('math.affine_089')
def affine_089(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 2 + 2) * scale

@task('math.affine_090')
def affine_090(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 3 + 3) * scale

@task('math.affine_091')
def affine_091(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 4 + -3) * scale

@task('math.affine_092')
def affine_092(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 5 + -2) * scale

@task('math.affine_093')
def affine_093(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 6 + -1) * scale

@task('math.affine_094')
def affine_094(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 7 + 0) * scale

@task('math.affine_095')
def affine_095(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 8 + 1) * scale

@task('math.affine_096')
def affine_096(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 9 + 2) * scale

@task('math.affine_097')
def affine_097(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 10 + 3) * scale

@task('math.affine_098')
def affine_098(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 11 + -3) * scale

@task('math.affine_099')
def affine_099(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 1 + -2) * scale

@task('math.affine_100')
def affine_100(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 2 + -1) * scale

@task('math.affine_101')
def affine_101(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 3 + 0) * scale

@task('math.affine_102')
def affine_102(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 4 + 1) * scale

@task('math.affine_103')
def affine_103(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 5 + 2) * scale

@task('math.affine_104')
def affine_104(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 6 + 3) * scale

@task('math.affine_105')
def affine_105(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 7 + -3) * scale

@task('math.affine_106')
def affine_106(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 8 + -2) * scale

@task('math.affine_107')
def affine_107(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 9 + -1) * scale

@task('math.affine_108')
def affine_108(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 10 + 0) * scale

@task('math.affine_109')
def affine_109(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 11 + 1) * scale

@task('math.affine_110')
def affine_110(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 1 + 2) * scale

@task('math.affine_111')
def affine_111(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 2 + 3) * scale

@task('math.affine_112')
def affine_112(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 3 + -3) * scale

@task('math.affine_113')
def affine_113(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 4 + -2) * scale

@task('math.affine_114')
def affine_114(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 5 + -1) * scale

@task('math.affine_115')
def affine_115(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 6 + 0) * scale

@task('math.affine_116')
def affine_116(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 7 + 1) * scale

@task('math.affine_117')
def affine_117(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 8 + 2) * scale

@task('math.affine_118')
def affine_118(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 9 + 3) * scale

@task('math.affine_119')
def affine_119(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 10 + -3) * scale

@task('math.affine_120')
def affine_120(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 11 + -2) * scale

@task('math.affine_121')
def affine_121(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 1 + -1) * scale

@task('math.affine_122')
def affine_122(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 2 + 0) * scale

@task('math.affine_123')
def affine_123(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 3 + 1) * scale

@task('math.affine_124')
def affine_124(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 4 + 2) * scale

@task('math.affine_125')
def affine_125(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 5 + 3) * scale

@task('math.affine_126')
def affine_126(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 6 + -3) * scale

@task('math.affine_127')
def affine_127(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 7 + -2) * scale

@task('math.affine_128')
def affine_128(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 8 + -1) * scale

@task('math.affine_129')
def affine_129(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 9 + 0) * scale

@task('math.affine_130')
def affine_130(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 10 + 1) * scale

@task('math.affine_131')
def affine_131(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 11 + 2) * scale

@task('math.affine_132')
def affine_132(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 1 + 3) * scale

@task('math.affine_133')
def affine_133(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 2 + -3) * scale

@task('math.affine_134')
def affine_134(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 3 + -2) * scale

@task('math.affine_135')
def affine_135(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 4 + -1) * scale

@task('math.affine_136')
def affine_136(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 5 + 0) * scale

@task('math.affine_137')
def affine_137(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 6 + 1) * scale

@task('math.affine_138')
def affine_138(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 7 + 2) * scale

@task('math.affine_139')
def affine_139(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 8 + 3) * scale

@task('math.affine_140')
def affine_140(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 9 + -3) * scale

@task('math.affine_141')
def affine_141(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 10 + -2) * scale

@task('math.affine_142')
def affine_142(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 11 + -1) * scale

@task('math.affine_143')
def affine_143(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 1 + 0) * scale

@task('math.affine_144')
def affine_144(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 2 + 1) * scale

@task('math.affine_145')
def affine_145(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 3 + 2) * scale

@task('math.affine_146')
def affine_146(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 4 + 3) * scale

@task('math.affine_147')
def affine_147(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 5 + -3) * scale

@task('math.affine_148')
def affine_148(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 6 + -2) * scale

@task('math.affine_149')
def affine_149(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 7 + -1) * scale

@task('math.affine_150')
def affine_150(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 8 + 0) * scale

@task('math.affine_151')
def affine_151(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 9 + 1) * scale

@task('math.affine_152')
def affine_152(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 10 + 2) * scale

@task('math.affine_153')
def affine_153(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 11 + 3) * scale

@task('math.affine_154')
def affine_154(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 1 + -3) * scale

@task('math.affine_155')
def affine_155(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 2 + -2) * scale

@task('math.affine_156')
def affine_156(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 3 + -1) * scale

@task('math.affine_157')
def affine_157(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 4 + 0) * scale

@task('math.affine_158')
def affine_158(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 5 + 1) * scale

@task('math.affine_159')
def affine_159(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 6 + 2) * scale

@task('math.affine_160')
def affine_160(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 7 + 3) * scale

@task('math.affine_161')
def affine_161(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 8 + -3) * scale

@task('math.affine_162')
def affine_162(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 9 + -2) * scale

@task('math.affine_163')
def affine_163(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 10 + -1) * scale

@task('math.affine_164')
def affine_164(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 11 + 0) * scale

@task('math.affine_165')
def affine_165(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 1 + 1) * scale

@task('math.affine_166')
def affine_166(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 2 + 2) * scale

@task('math.affine_167')
def affine_167(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 3 + 3) * scale

@task('math.affine_168')
def affine_168(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 4 + -3) * scale

@task('math.affine_169')
def affine_169(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 5 + -2) * scale

@task('math.affine_170')
def affine_170(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 6 + -1) * scale

@task('math.affine_171')
def affine_171(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 7 + 0) * scale

@task('math.affine_172')
def affine_172(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 8 + 1) * scale

@task('math.affine_173')
def affine_173(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 9 + 2) * scale

@task('math.affine_174')
def affine_174(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 10 + 3) * scale

@task('math.affine_175')
def affine_175(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 11 + -3) * scale

@task('math.affine_176')
def affine_176(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 1 + -2) * scale

@task('math.affine_177')
def affine_177(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 2 + -1) * scale

@task('math.affine_178')
def affine_178(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 3 + 0) * scale

@task('math.affine_179')
def affine_179(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 4 + 1) * scale

@task('math.affine_180')
def affine_180(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 5 + 2) * scale

@task('math.affine_181')
def affine_181(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 6 + 3) * scale

@task('math.affine_182')
def affine_182(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 7 + -3) * scale

@task('math.affine_183')
def affine_183(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 8 + -2) * scale

@task('math.affine_184')
def affine_184(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 9 + -1) * scale

@task('math.affine_185')
def affine_185(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 10 + 0) * scale

@task('math.affine_186')
def affine_186(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 11 + 1) * scale

@task('math.affine_187')
def affine_187(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 1 + 2) * scale

@task('math.affine_188')
def affine_188(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 2 + 3) * scale

@task('math.affine_189')
def affine_189(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 3 + -3) * scale

@task('math.affine_190')
def affine_190(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 4 + -2) * scale

@task('math.affine_191')
def affine_191(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 5 + -1) * scale

@task('math.affine_192')
def affine_192(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 6 + 0) * scale

@task('math.affine_193')
def affine_193(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 7 + 1) * scale

@task('math.affine_194')
def affine_194(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 8 + 2) * scale

@task('math.affine_195')
def affine_195(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 9 + 3) * scale

@task('math.affine_196')
def affine_196(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 10 + -3) * scale

@task('math.affine_197')
def affine_197(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 11 + -2) * scale

@task('math.affine_198')
def affine_198(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 1 + -1) * scale

@task('math.affine_199')
def affine_199(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 2 + 0) * scale

@task('math.affine_200')
def affine_200(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 3 + 1) * scale

@task('math.affine_201')
def affine_201(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 4 + 2) * scale

@task('math.affine_202')
def affine_202(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 5 + 3) * scale

@task('math.affine_203')
def affine_203(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 6 + -3) * scale

@task('math.affine_204')
def affine_204(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 7 + -2) * scale

@task('math.affine_205')
def affine_205(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 8 + -1) * scale

@task('math.affine_206')
def affine_206(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 9 + 0) * scale

@task('math.affine_207')
def affine_207(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 10 + 1) * scale

@task('math.affine_208')
def affine_208(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 11 + 2) * scale

@task('math.affine_209')
def affine_209(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 1 + 3) * scale

@task('math.affine_210')
def affine_210(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 2 + -3) * scale

@task('math.affine_211')
def affine_211(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 3 + -2) * scale

@task('math.affine_212')
def affine_212(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 4 + -1) * scale

@task('math.affine_213')
def affine_213(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 5 + 0) * scale

@task('math.affine_214')
def affine_214(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 6 + 1) * scale

@task('math.affine_215')
def affine_215(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 7 + 2) * scale

@task('math.affine_216')
def affine_216(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 8 + 3) * scale

@task('math.affine_217')
def affine_217(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 9 + -3) * scale

@task('math.affine_218')
def affine_218(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 10 + -2) * scale

@task('math.affine_219')
def affine_219(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 11 + -1) * scale

@task('math.affine_220')
def affine_220(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 1 + 0) * scale

@task('math.affine_221')
def affine_221(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 2 + 1) * scale

@task('math.affine_222')
def affine_222(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 3 + 2) * scale

@task('math.affine_223')
def affine_223(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 4 + 3) * scale

@task('math.affine_224')
def affine_224(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 5 + -3) * scale

@task('math.affine_225')
def affine_225(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 6 + -2) * scale

@task('math.affine_226')
def affine_226(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 7 + -1) * scale

@task('math.affine_227')
def affine_227(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 8 + 0) * scale

@task('math.affine_228')
def affine_228(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 9 + 1) * scale

@task('math.affine_229')
def affine_229(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 10 + 2) * scale

@task('math.affine_230')
def affine_230(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 11 + 3) * scale

@task('math.affine_231')
def affine_231(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 1 + -3) * scale

@task('math.affine_232')
def affine_232(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 2 + -2) * scale

@task('math.affine_233')
def affine_233(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 3 + -1) * scale

@task('math.affine_234')
def affine_234(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 4 + 0) * scale

@task('math.affine_235')
def affine_235(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 5 + 1) * scale

@task('math.affine_236')
def affine_236(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 6 + 2) * scale

@task('math.affine_237')
def affine_237(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 7 + 3) * scale

@task('math.affine_238')
def affine_238(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 8 + -3) * scale

@task('math.affine_239')
def affine_239(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 9 + -2) * scale

@task('math.affine_240')
def affine_240(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 10 + -1) * scale

@task('math.affine_241')
def affine_241(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 11 + 0) * scale

@task('math.affine_242')
def affine_242(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 1 + 1) * scale

@task('math.affine_243')
def affine_243(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 2 + 2) * scale

@task('math.affine_244')
def affine_244(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 3 + 3) * scale

@task('math.affine_245')
def affine_245(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 4 + -3) * scale

@task('math.affine_246')
def affine_246(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 5 + -2) * scale

@task('math.affine_247')
def affine_247(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 6 + -1) * scale

@task('math.affine_248')
def affine_248(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 7 + 0) * scale

@task('math.affine_249')
def affine_249(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 8 + 1) * scale

@task('math.affine_250')
def affine_250(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 9 + 2) * scale

@task('math.affine_251')
def affine_251(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 10 + 3) * scale

@task('math.affine_252')
def affine_252(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 11 + -3) * scale

@task('math.affine_253')
def affine_253(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 1 + -2) * scale

@task('math.affine_254')
def affine_254(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 2 + -1) * scale

@task('math.affine_255')
def affine_255(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 3 + 0) * scale

@task('math.affine_256')
def affine_256(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 4 + 1) * scale

@task('math.affine_257')
def affine_257(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 5 + 2) * scale

@task('math.affine_258')
def affine_258(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 6 + 3) * scale

@task('math.affine_259')
def affine_259(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 7 + -3) * scale

@task('math.affine_260')
def affine_260(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 8 + -2) * scale

@task('math.affine_261')
def affine_261(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 9 + -1) * scale

@task('math.affine_262')
def affine_262(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 10 + 0) * scale

@task('math.affine_263')
def affine_263(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 11 + 1) * scale

@task('math.affine_264')
def affine_264(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 1 + 2) * scale

@task('math.affine_265')
def affine_265(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 2 + 3) * scale

@task('math.affine_266')
def affine_266(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 3 + -3) * scale

@task('math.affine_267')
def affine_267(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 4 + -2) * scale

@task('math.affine_268')
def affine_268(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 5 + -1) * scale

@task('math.affine_269')
def affine_269(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 6 + 0) * scale

@task('math.affine_270')
def affine_270(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 7 + 1) * scale

@task('math.affine_271')
def affine_271(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 8 + 2) * scale

@task('math.affine_272')
def affine_272(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 9 + 3) * scale

@task('math.affine_273')
def affine_273(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 10 + -3) * scale

@task('math.affine_274')
def affine_274(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 11 + -2) * scale

@task('math.affine_275')
def affine_275(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 1 + -1) * scale

@task('math.affine_276')
def affine_276(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 2 + 0) * scale

@task('math.affine_277')
def affine_277(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 3 + 1) * scale

@task('math.affine_278')
def affine_278(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 4 + 2) * scale

@task('math.affine_279')
def affine_279(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 5 + 3) * scale

@task('math.affine_280')
def affine_280(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 6 + -3) * scale

@task('math.affine_281')
def affine_281(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 7 + -2) * scale

@task('math.affine_282')
def affine_282(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 8 + -1) * scale

@task('math.affine_283')
def affine_283(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 9 + 0) * scale

@task('math.affine_284')
def affine_284(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 10 + 1) * scale

@task('math.affine_285')
def affine_285(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 11 + 2) * scale

@task('math.affine_286')
def affine_286(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 1 + 3) * scale

@task('math.affine_287')
def affine_287(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 2 + -3) * scale

@task('math.affine_288')
def affine_288(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 3 + -2) * scale

@task('math.affine_289')
def affine_289(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 4 + -1) * scale

@task('math.affine_290')
def affine_290(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 5 + 0) * scale

@task('math.affine_291')
def affine_291(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 6 + 1) * scale

@task('math.affine_292')
def affine_292(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 7 + 2) * scale

@task('math.affine_293')
def affine_293(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 8 + 3) * scale

@task('math.affine_294')
def affine_294(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 9 + -3) * scale

@task('math.affine_295')
def affine_295(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 10 + -2) * scale

@task('math.affine_296')
def affine_296(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 11 + -1) * scale

@task('math.affine_297')
def affine_297(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 1 + 0) * scale

@task('math.affine_298')
def affine_298(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 2 + 1) * scale

@task('math.affine_299')
def affine_299(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 3 + 2) * scale

@task('math.affine_300')
def affine_300(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 4 + 3) * scale

@task('math.affine_301')
def affine_301(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 5 + -3) * scale

@task('math.affine_302')
def affine_302(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 6 + -2) * scale

@task('math.affine_303')
def affine_303(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 7 + -1) * scale

@task('math.affine_304')
def affine_304(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 8 + 0) * scale

@task('math.affine_305')
def affine_305(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 9 + 1) * scale

@task('math.affine_306')
def affine_306(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 10 + 2) * scale

@task('math.affine_307')
def affine_307(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 11 + 3) * scale

@task('math.affine_308')
def affine_308(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 1 + -3) * scale

@task('math.affine_309')
def affine_309(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 2 + -2) * scale

@task('math.affine_310')
def affine_310(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 3 + -1) * scale

@task('math.affine_311')
def affine_311(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 4 + 0) * scale

@task('math.affine_312')
def affine_312(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 5 + 1) * scale

@task('math.affine_313')
def affine_313(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 6 + 2) * scale

@task('math.affine_314')
def affine_314(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 7 + 3) * scale

@task('math.affine_315')
def affine_315(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 8 + -3) * scale

@task('math.affine_316')
def affine_316(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 9 + -2) * scale

@task('math.affine_317')
def affine_317(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 10 + -1) * scale

@task('math.affine_318')
def affine_318(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 11 + 0) * scale

@task('math.affine_319')
def affine_319(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 1 + 1) * scale

@task('math.affine_320')
def affine_320(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 2 + 2) * scale

@task('math.affine_321')
def affine_321(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 3 + 3) * scale

@task('math.affine_322')
def affine_322(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 4 + -3) * scale

@task('math.affine_323')
def affine_323(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 5 + -2) * scale

@task('math.affine_324')
def affine_324(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 6 + -1) * scale

@task('math.affine_325')
def affine_325(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 7 + 0) * scale

@task('math.affine_326')
def affine_326(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 8 + 1) * scale

@task('math.affine_327')
def affine_327(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 9 + 2) * scale

@task('math.affine_328')
def affine_328(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 10 + 3) * scale

@task('math.affine_329')
def affine_329(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 11 + -3) * scale

@task('math.affine_330')
def affine_330(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 1 + -2) * scale

@task('math.affine_331')
def affine_331(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 2 + -1) * scale

@task('math.affine_332')
def affine_332(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 3 + 0) * scale

@task('math.affine_333')
def affine_333(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 4 + 1) * scale

@task('math.affine_334')
def affine_334(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 5 + 2) * scale

@task('math.affine_335')
def affine_335(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 6 + 3) * scale

@task('math.affine_336')
def affine_336(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 7 + -3) * scale

@task('math.affine_337')
def affine_337(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 8 + -2) * scale

@task('math.affine_338')
def affine_338(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 9 + -1) * scale

@task('math.affine_339')
def affine_339(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 10 + 0) * scale

@task('math.affine_340')
def affine_340(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 11 + 1) * scale

@task('math.affine_341')
def affine_341(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 1 + 2) * scale

@task('math.affine_342')
def affine_342(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 2 + 3) * scale

@task('math.affine_343')
def affine_343(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 3 + -3) * scale

@task('math.affine_344')
def affine_344(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 4 + -2) * scale

@task('math.affine_345')
def affine_345(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 5 + -1) * scale

@task('math.affine_346')
def affine_346(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 6 + 0) * scale

@task('math.affine_347')
def affine_347(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 7 + 1) * scale

@task('math.affine_348')
def affine_348(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 8 + 2) * scale

@task('math.affine_349')
def affine_349(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 9 + 3) * scale

@task('math.affine_350')
def affine_350(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 10 + -3) * scale

@task('math.affine_351')
def affine_351(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 11 + -2) * scale

@task('math.affine_352')
def affine_352(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 1 + -1) * scale

@task('math.affine_353')
def affine_353(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 2 + 0) * scale

@task('math.affine_354')
def affine_354(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 3 + 1) * scale

@task('math.affine_355')
def affine_355(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 4 + 2) * scale

@task('math.affine_356')
def affine_356(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 5 + 3) * scale

@task('math.affine_357')
def affine_357(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 6 + -3) * scale

@task('math.affine_358')
def affine_358(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 7 + -2) * scale

@task('math.affine_359')
def affine_359(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 8 + -1) * scale

@task('math.affine_360')
def affine_360(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 9 + 0) * scale

@task('math.affine_361')
def affine_361(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 10 + 1) * scale

@task('math.affine_362')
def affine_362(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 11 + 2) * scale

@task('math.affine_363')
def affine_363(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 1 + 3) * scale

@task('math.affine_364')
def affine_364(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 2 + -3) * scale

@task('math.affine_365')
def affine_365(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 3 + -2) * scale

@task('math.affine_366')
def affine_366(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 4 + -1) * scale

@task('math.affine_367')
def affine_367(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 5 + 0) * scale

@task('math.affine_368')
def affine_368(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 6 + 1) * scale

@task('math.affine_369')
def affine_369(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 7 + 2) * scale

@task('math.affine_370')
def affine_370(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 8 + 3) * scale

@task('math.affine_371')
def affine_371(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 9 + -3) * scale

@task('math.affine_372')
def affine_372(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 10 + -2) * scale

@task('math.affine_373')
def affine_373(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 11 + -1) * scale

@task('math.affine_374')
def affine_374(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 1 + 0) * scale

@task('math.affine_375')
def affine_375(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 2 + 1) * scale

@task('math.affine_376')
def affine_376(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 3 + 2) * scale

@task('math.affine_377')
def affine_377(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 4 + 3) * scale

@task('math.affine_378')
def affine_378(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 5 + -3) * scale

@task('math.affine_379')
def affine_379(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 6 + -2) * scale

@task('math.affine_380')
def affine_380(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 7 + -1) * scale

@task('math.affine_381')
def affine_381(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 8 + 0) * scale

@task('math.affine_382')
def affine_382(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 9 + 1) * scale

@task('math.affine_383')
def affine_383(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 10 + 2) * scale

@task('math.affine_384')
def affine_384(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 11 + right * 11 + 3) * scale

@task('math.affine_385')
def affine_385(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 12 + right * 1 + -3) * scale

@task('math.affine_386')
def affine_386(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 13 + right * 2 + -2) * scale

@task('math.affine_387')
def affine_387(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 14 + right * 3 + -1) * scale

@task('math.affine_388')
def affine_388(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 15 + right * 4 + 0) * scale

@task('math.affine_389')
def affine_389(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 16 + right * 5 + 1) * scale

@task('math.affine_390')
def affine_390(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 17 + right * 6 + 2) * scale

@task('math.affine_391')
def affine_391(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 1 + right * 7 + 3) * scale

@task('math.affine_392')
def affine_392(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 2 + right * 8 + -3) * scale

@task('math.affine_393')
def affine_393(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 3 + right * 9 + -2) * scale

@task('math.affine_394')
def affine_394(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 4 + right * 10 + -1) * scale

@task('math.affine_395')
def affine_395(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 5 + right * 11 + 0) * scale

@task('math.affine_396')
def affine_396(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 6 + right * 1 + 1) * scale

@task('math.affine_397')
def affine_397(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 7 + right * 2 + 2) * scale

@task('math.affine_398')
def affine_398(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 8 + right * 3 + 3) * scale

@task('math.affine_399')
def affine_399(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 9 + right * 4 + -3) * scale

@task('math.affine_400')
def affine_400(**kwargs: object) -> float:
    left = float(kwargs.get('left', 0))
    right = float(kwargs.get('right', 0))
    scale = float(kwargs.get('scale', 1))
    return (left * 10 + right * 5 + -2) * scale

