# Agenda (cron)

Five fields: minute hour day-of-month month day-of-week.
Names `jan`..`dec` and `sun`..`sat` are accepted. `*/n` steps work.
When both day-of-month and day-of-week are restricted, either matching fires.
