# Layout

`domain` holds JobSpec and the DAG. `ports` are protocols. Adapters implement
YAML, clocks, and stores. `application.Engine` walks ready steps. `Pipeline`
loads, runs, and optionally schedules.
