name: nightly-backup
schedule: "30 2 * * *"
timezone: UTC
label: dumps then uploads
tenant: default
steps:
  dump:
    call: core.echo
    message: dump
    retries: 2
    retry_seconds: 1
  upload:
    call: core.echo
    message: upload
    needs: [dump]
